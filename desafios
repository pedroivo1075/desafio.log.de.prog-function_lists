1. Crie uma função que calcule o índice de massa corporal (IMC) de uma pessoa, a partir de sua altura, em metros, e peso, em quilogramas, que serão recebidos como parâmetro.
>>IMC = peso / altura²<<

function calculoIMC(peso, altura) {
  return peso / (altura * altura);
}
let resultado = calculoIMC(82, 1.77);
console.log(resultado.toFixed(2));

2. Crie uma função que calcule o valor do fatorial de um número passado como parâmetro.

function calculoFatorial(numero){
  let resultado = 1;
  for (let x = numero; x > 0; x = x- 1){
    resultado = resultado * x;
  }
  console.log(resulado);
}
calculoFatorial(7);

3. Crie uma função que converte um valor em dólar, passado como parâmetro, e retorna o valor equivalente em reais. Para isso, considere a cotação do dólar igual a R$4,80.

function cambioDolar(dolar){
  let cotacao = 4.80;
  return dolar * cotacao;
}
let real = cambioDolar(10);
console.log(real);

4. Crie uma função que mostre na tela a área e o perímetro de uma sala retangular, utilizando altura e largura que serão dadas como parâmetro.

let altura = prompt('Qual a altura da sala? (Em metros)');
let largura= prompt('Qual a largura da sala? (Em metros)');

function infosRetangulo(altura, largura){
  let perimetro = (altura * 2 + largura * 2);
  let area = altura * lagura;
  return alert('A sala retangular tem ' + perímetro + 'm de perimetro e ' + area + ' m² de área.');
}
infosRetangulo(altura, largura);

5. Crie uma função que mostre na tela a área e o perímetro de uma sala circular, utilizando seu raio que será fornecido como parâmetro. Considere Pi = 3,14.

let pi = 3.14

function infosCirculo(raio){
  let perimetro = 2 * pi * raio;
  let area = pi * (raio * raio);
  return alert(`O perímetro do circulo é de ${perimetro}m e sua area é igual a ${area}m²`);
}

6. Crie uma função que mostre na tela a tabuada de um número dado como parâmetro.

function tabuada(numero){
  let multiplicador = 1;
  while (multiplicador >= 10);
  let resultado = numero * multiplicador;
  return alert(`${numero} * ${multiplicador} = ${resultado}`);
  multiplicador = multiplicador + 1;
}

tabuada(5);
